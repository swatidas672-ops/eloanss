node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Bun lockfile - this project uses npm (package-lock.json).
# A stray bun.lock makes Cloudflare Pages run `bun install --frozen-lockfile`,
# which fails on lockfile version mismatches.
bun.lock
bun.lockb
